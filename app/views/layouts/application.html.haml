!!!
%html
  %head
    %title= yield(:title) + " - Nate McGarigal Photography & Cinematography"
    = stylesheet_link_tag    "application", media: "all"
    = stylesheet_link_tag    "http://fonts.googleapis.com/css?family=Lato:300,400,300italic", media: "all"
    
  
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    - if on_detail_page?
      #detail-page.clearfix
        = render "shared/detail_page_menu"
        = yield
    - else
      = render "shared/primary_menu"
      = yield
      = render "shared/footer"
      